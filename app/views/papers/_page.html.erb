<div class="papers">
<div class="page">
  <% @papers.each do |paper| %>
    <%= render partial: "item", locals: { paper: paper, comment: paper.top_comment } %>
  <% end %>
  </div>
</div>

<%= paginate @papers %>